# 任意精度时长计算核心

## 目标

网页端时长算术不再受 `Number.MAX_SAFE_INTEGER` 的固定数值上限约束。计算核心统一使用 **BigInt 整数毫秒**，用户输入先以十进制字符串保存，再精确换算成毫秒。

当前纯计算实现已经从页面逻辑中独立到 `js/duration-precision.js`。该文件不依赖 DOM、localStorage、主题、日期选择器或页面状态，可以单独测试，也作为未来 ArkTS / HarmonyOS 迁移的参考实现。

日期映射也已经从日期 UI 中独立到 `js/date-mapper.js`：纯时长核心只产出整数毫秒，Date Mapper 再负责本地日期验证、平台日期范围判断和结束年月日映射。

跨平台的正式语义以 `docs/DURATION_CORE_CONTRACT.md` 为准。

## 核心规则

1. 算术核心单位：`BigInt` 毫秒。
2. 用户输入在进入核心前保持字符串，不先转 `Number`。
3. `天 / 时 / 分 / 秒` 的十进制小数只有在能**精确落到整数毫秒**时才接受。
4. 最小时间精度为 `1ms`，不做静默四舍五入。
5. 单个数字最多 100 位。这是资源保护，不是数学数值上限。
6. `+ / -`、总结果、天时分秒拆分、`H:MM:SS` 均使用 BigInt 运算。
7. `小时 / 分` 的十进制显示使用 BigInt 长除法生成，默认最多显示 6 位小数；这只是显示精度，不影响内部精确值。
8. 历史记录中的 `resultMs` 统一保存为十进制字符串；旧版 Number 历史读取时自动迁移。
9. 日期映射是独立能力：时长可以任意大，但只有目标毫秒落在平台 Date 可表示范围内时才生成结束年月日。
10. 点击“日期”默认加入设备本地当天 `00:00`；不得把点击瞬间的当前时分当作默认基准。
11. 超长主结果不得换行或截断关键数字：先降低到可读字号下限，仍超宽时允许结果区域横向查看。
12. 冒号输入、单位片段和多行表达式求值属于纯核心的一部分，页面只负责编辑状态和交互。
13. 表达式、主结果、辅助时长和历史中的超长精确值不得使用省略号隐藏关键数字；真实溢出时保留可读字号并允许横向查看。

## 示例

- `300000000天`：允许，精确值为 `25920000000000000ms`。
- `300000000天 + 0.001秒`：结果比前者严格多 `1ms`，不会被浮点数吞掉。
- `1.5小时`：精确换算为 `5400000ms`。
- `0.0001分钟`：精确换算为 `6ms`。
- `0.0001秒`：拒绝，因为等于 `0.1ms`，低于当前 1ms 精度。
- 超大时长 + 日期：时长仍正常计算；若目标日期超出浏览器日期范围，仅显示“结束日期超出范围”。

## 自动测试

仓库根目录执行：

```bash
npm test
```

自动测试不依赖真实浏览器即可检查三层能力：

- `tests/duration-precision.test.cjs`：直接加载 `js/duration-precision.js`，验证任意精度算术与跨平台共享向量；
- `tests/date-mapper.test.cjs`：验证当天零点、合法/非法日期、正负毫秒映射以及日期越界不反向否定时长结果；
- `tests/large-number-ui.test.cjs`：验证超长数值只有在真实溢出时才启用横向查看，短数值保持普通展示，并保留完整值的可访问说明。

`tests/duration-core-vectors.json` 保存跨平台共享测试向量。未来 ArkTS 核心应使用同一批输入，并逐项比较最终整数毫秒字符串。

## 回归测试门

每次修改计算核心至少检查：

- 普通整数：`1天 + 2小时 - 30分`；
- 毫秒边界：`0.001秒`；
- 不可精确小数：`0.0001秒`；
- 可精确小数：`1.5小时`、`0.0001分钟`；
- 大数：`300000000天`；
- 极大数：至少 80–100 位整数；
- 大数 + 1ms 不丢失；
- 负结果继续运算；
- 超大 `H:MM:SS` 冒号输入；
- 天时分秒 / 小时十进制 / 小时60进制 / 分显示；
- 历史保存、恢复、旧历史迁移；
- 日期按钮默认当天 00:00；
- 日期范围内映射与日期范围外降级；
- 超长表达式、主结果和历史记录仍能查看完整精确值。
