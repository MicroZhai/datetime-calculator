# 时间计算器响应式布局计划

> 基线：Zhai Design Language v1.2 / Universal Template v1.3 / Responsive Tokens。
>
> 目标：不同手机、平板、电脑、横竖屏和分屏窗口中，保持同一计算任务与交互语义；空间变化时重组关系，而不是整体缩放。

## 1. 核心原则

1. 不按设备型号判断布局，只看当前窗口 Profile。
2. Profile = Width + Height + Orientation + Input + Window Mode。
3. 宽度主要决定 Composition，高度主要决定 Density；两者共同决定 Transform。
4. 手机优先保证当前结果、完整计算键盘和当前表达式上下文可用。
5. 空间不足时先释放空占位、说明和冗余 Metadata，再让长表达式内部滚动；最后才降低到 40px 触控底线。
6. 不能为了“一屏全见”整体缩放页面、缩小字体到不可读或把按键缩到低于 40px。
7. 空余高度属于 Display Workspace：结果自然落在显示区下部，键盘靠近视区底部；禁止内容挤在上半屏而底部留下无职责大空洞。
8. 宽屏允许 `Stack → Split`，把 Expression / Result 与 Keypad 左右并列；这仍是同一计算任务，不新增第二工作区或桌面侧栏。
9. 字体放大、浏览器缩放或极端窗口下允许 Reflow / 必要滚动，不通过禁止缩放维持版式。

## 2. Width Profile

沿用 Zhai Design min-width 断点：

- `xs <320`
- `sm >=320`
- `md >=600`
- `lg >=840`
- `xl >=1440`

项目行为：

- xs：保持手机单列；减少横向 chrome 与间距，触控区仍 >=40px。
- sm：默认手机 Stack。
- md `600–839`：正常 / 高窗口仍为单任务居中 Stack，工作区宽度最多约 600px；短窗口转 Split。
- lg `>=840`：允许同一任务直接使用左右 Split，避免把手机单列无限拉高或在大屏中心形成过小的“手机截图”。
- xl：延续 Split；不因为超宽自动增加第三栏或无职责 Rail。

## 3. Height Strategy

Height Profile 仍用于说明优先级，但实现不再使用突变式 `44 / 48 / 56` 三档硬切。常见窗口使用 `clamp()` 流体调整标题栏、结果字号、键盘行高、间距和表达式最小高度。

### Regular `>=820px`

- 保留底部说明与次级 `HH:MM:SS`；
- 键盘自然趋近 56px 行高；
- 表达式可增长到正常上限，超过上限后内部滚动；
- 手机 Display Workspace 可吸收多余高度；
- md 竖屏使用约 650px 的舒适工作区上限，避免平板被拉成巨大空画布。

### Compact `680–819px`

- 底部说明隐藏；
- App 底部空白压缩；
- 结果、格式切换、键盘行高和间距通过流体值自然收紧；
- 空的规范化 / 错误区域不占位；
- 次级 `HH:MM:SS` 默认仍保留。

### Short `<680px`

手机：

- 表达式最大约 96px，通常只暴露约两行；更多表达式只在 Expression 内部滚动；
- 键盘保持完整可见；
- 结果和格式切换仍保留。

md+：

- 转为左右 Split，用横向空间换回纵向空间；
- Expression / Result 与 Keypad 同时可见。

### Extreme Short Phone `<580px`

- 次级 `HH:MM:SS` 隐藏；
- 表达式最大约 80px；
- 键盘行高约 42px，实际触控区仍 >=40px；
- 行编辑动作保持一行并允许自身横向滚动。

## 4. Region Priority

空间不足时按以下顺序处理：

1. 当前结果：必须保留。
2. 数字 / 日期 / 单位 / + / − / = / 退格等计算动作：必须保留。
3. 当前表达式：必须可见，但允许内部滚动。
4. 已启用的基准时间 / 结束时间：保留为低强调 Metadata。
5. 格式切换：保留，可流体压缩。
6. 状态提示：保留必要文本，不为默认状态预留固定大空白。
7. 辅助 `HH:MM:SS`：只在 Extreme Short Phone 降级。
8. 底部使用说明：Compact / Short 隐藏。
9. 装饰：最先消失。

## 5. Transform Matrix

| Window | Layout | Expression | Keypad | Secondary |
|---|---|---|---|---|
| sm + Regular | Stack | 正常上限，内部滚动 | 流体至 56px | 全部 |
| sm + Compact | Stack | 流体收紧 | 流体 48–56px | 隐藏说明 |
| sm + Short | Stack | max ~96px，内部滚动 | >=48px | 隐藏说明 |
| sm + Extreme Short | Stack | max ~80px，内部滚动 | ~42px / hit >=40 | 隐藏 HH:MM:SS / 说明 |
| md portrait + Regular | 居中 Stack，work area max ~650px | 正常 | 正常 | 全部 |
| md+ + Short | Split | 左侧局部滚动 | 右侧完整键盘 | 保留必要 Metadata |
| lg+ | Split | 左侧 | 右侧 | 保持语义层级 |

## 6. 已执行视觉 / 几何验证（2026-08-09）

测试了两类页面状态：

- Empty：空表达式、`0分`；
- Busy：3 行表达式 + 基准时间 `YYYY/MM/DD HH:mm` + 结束时间 + 长时长结果。

实测视口：

- `320 × 568`
- `347 × 610`（接近用户当前手机浏览器可用视区）
- `360 × 640`
- `360 × 780`
- `390 × 844`
- `600 × 600`
- `768 × 1024`
- `840 × 480`
- `1024 × 768`
- `1440 × 900`

最终验证结果：

- Empty / Busy 均无文档级纵向溢出；
- Keypad 在上述窗口均完整留在可视区；
- 320×568 / 347×610 / 360×640 的 Busy 状态通过 Expression 内部滚动吸收额外行数；
- 600×600 / 840×480 等宽短窗口使用 Split 后核心操作完整；
- md 竖屏使用工作区高度上限，避免巨大内部空洞；
- lg / desktop 使用 Split，不把单列手机布局无限放大。

## 7. 每次改动的验收要求

至少检查：

- 核心键盘是否完整可操作；
- `=` 是否仍是唯一 Primary；
- 当前结果是否始终清楚；
- 长表达式是否只在 Expression 内部滚动；
- 基准时间 / 结束时间是否保持单行 Metadata；
- 行编辑状态是否不会把整个页面撑高；
- safe area 是否避让；
- Light / Dark 是否层级等价；
- 浏览器缩放 / 字体放大时是否仍能通过 Reflow 完成任务。

## 8. 非目标

- 不追求所有设备完全相同的几何位置。
- 不追求任何极端窗口都“所有次级信息一屏全见”。
- 不按手机 / 平板 / PC 写三套业务页面。
- 不为了适配短屏把按键缩到低于 40px。
- 不因为桌面宽屏就添加无职责 SideBar、Card 或额外信息栏。