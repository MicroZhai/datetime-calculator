# 时间计算器响应式布局计划

> 基线：Zhai Design Language v1.2 / Universal Template v1.3 / Responsive Tokens。
>
> 目标：不同手机、平板、电脑、横竖屏和分屏窗口中，保持同一计算任务与交互语义；空间变化时重组关系，而不是整体缩放。

## 1. 核心原则

1. 不按设备型号判断布局，只看当前窗口 Profile。
2. Profile = Width + Height + Orientation + Input + Window Mode。
3. 宽度决定“怎么排”；高度决定“显示多少”。
4. 手机优先保证完整计算键盘和当前结果可用。
5. 空间不足时先移除冗余说明和空占位，再压缩间距，再降级 Metadata；不先缩小触控区和正文可读性。
6. 长表达式使用表达式区域内部滚动，不持续把键盘推出视区。
7. 宽且短的窗口允许 `Stack → Split`，把表达式/结果与键盘左右并列；这不是新增第二工作区，也不是桌面侧栏。
8. 字体放大、浏览器缩放或极端窗口下允许页面发生 Reflow / 必要滚动，不通过禁止缩放维持版式。

## 2. Width Profile

沿用 Zhai Design min-width 断点：

- `xs <320`
- `sm >=320`
- `md >=600`
- `lg >=840`
- `xl >=1440`

项目行为：

- xs：单列；减少横向 chrome 与间距，触控区仍 >=40px。
- sm：默认手机单列。
- md / lg / xl：高度充足时仍保持单任务居中列，内容宽度最多约 600px；不因宽度增加自动添加侧栏。
- md 及以上且 Short：允许左右 Split，以横向空间换回纵向空间。

## 3. Height Profile

本项目增加三档高度状态：

### Regular `>=820px`

完整呈现：标题、状态、表达式、结果、辅助结果、格式切换、完整键盘、底部说明。

### Compact `680–819px`

保持完整核心功能：

- 标题栏适度压缩；
- 表达式最大高度降低并内部滚动；
- 空的规范化 / 错误区域不占位；
- 结果字号只做轻微降级；
- 格式切换压缩高度；
- 键盘行高降为 48px、间距降为 4px；
- 底部说明隐藏。

### Short `<680px`

优先保证任务完成：

- 继续保留结果、当前表达式上下文、全部计算键；
- 次级 `HH:MM:SS` 隐藏；
- 表达式成为主要局部滚动区域；
- 键盘行高 44px、实际触控区不得低于 40px；
- 行编辑动作保持一行，可横向滚动，不通过折行增加高度；
- 若同时 `width >=600px`，转为左右 Split。

## 4. Region Priority

空间不足时按以下顺序处理：

1. 当前结果：必须保留。
2. 数字 / 日期 / 单位 / + / − / = / 退格等计算动作：必须保留。
3. 当前表达式：必须可见，但允许内部滚动。
4. 已启用的基准时间 / 结束时间：保留为低强调 Metadata。
5. 格式切换：保留，但允许压缩。
6. 状态提示：保留必要文本，不为默认状态预留过多空白。
7. 辅助 `HH:MM:SS`：Short 可隐藏。
8. 底部使用说明：Compact / Short 隐藏。
9. 装饰：最先消失。

## 5. Transform Matrix

| Window | Layout | Expression | Keypad | Secondary |
|---|---|---|---|---|
| sm + Regular | Stack | 自然增长，max 200 | 56px 行高 | 全部 |
| sm + Compact | Stack | max 128 / 20dvh，内部滚动 | 48px 行高 | 隐藏底部说明 |
| sm + Short | Stack | max 92 / 18dvh，内部滚动 | 44px 行高 | 隐藏 HH:MM:SS / 说明 |
| md+ + Regular | 居中 Stack，max ~600 | 自然增长 | 正常 | 全部 |
| md+ + Compact | 居中 Stack | 紧凑 | 48px 行高 | 降级 |
| md+ + Short | Split | 左侧局部滚动 | 右侧完整键盘 | 降级 |

## 6. 验收矩阵

至少验证：

- 320 × 568
- 360 × 640
- 360 × 780
- 390 × 844
- 600 × 600
- 800 × 600
- 840 × 480 横屏 / 短窗
- 1024 × 768
- 1440 × 900

每个窗口至少检查：

- 核心键盘是否完整可操作；
- `=` 是否仍是唯一 Primary；
- 当前结果是否始终清楚；
- 长表达式是否只在表达式区域滚动；
- 基准时间 / 结束时间是否保持单行 Metadata；
- 行编辑状态是否不会把整个页面撑高；
- safe area 是否避让；
- Light / Dark 是否层级等价；
- 200% 浏览器缩放或字体放大时是否仍可完成任务。

## 7. 非目标

- 不追求所有设备完全相同的几何位置。
- 不追求任何极端窗口都“所有次级信息一屏全见”。
- 不按手机 / 平板 / PC 写三套业务页面。
- 不为了适配短屏把按键缩到低于 40px。
- 不因为桌面宽屏就添加无职责 SideBar、Card 或额外信息栏。
